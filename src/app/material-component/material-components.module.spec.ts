import { MaterialComponentsModule } from './material-components.module';

describe('MaterialComponentsModule', () => {
  it('should create an instance', () => {
    expect(new MaterialComponentsModule()).toBeTruthy();
  });
});
